export const calculateTrustScore = async (userId, connectionId) => {
  // Example: shared industries, number of mutual connections, etc.
  // Return a number between 0-100
  // Placeholder: always 70 for now
  return 70;
};
